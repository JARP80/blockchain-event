# testnet-box functionality
regtest=1
dnsseed=0
upnp=0

# listen on different ports than default 
listen=1

# always run a server, even with bitcoin-qt
server=1
# enable SSL for RPC server
#rpcssl=1
rpcallowip=0.0.0.0/0

rpcuser=rpcusername
rpcpassword=rpcpassword

# connect to node 1
addnode=127.0.0.1:8081

